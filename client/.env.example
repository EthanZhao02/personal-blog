# Local development example.
# Copy this file to client/.env for authorized local development only.

VITE_API_BASE_URL=http://localhost:8080
VITE_ROUTER_MODE=hash

# Optional AI chat endpoint. Keep API keys on the server side only.
# VITE_AI_CHAT_ENDPOINT=/api/ai/chat
